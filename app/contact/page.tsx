"use client";
import type { FormEvent } from "react";
import { useState } from "react";
import { localApi } from "@/lib/local-api";
export default function ContactPage(){const[message,setMessage]=useState("");const[loading,setLoading]=useState(false);async function submit(event:FormEvent<HTMLFormElement>){event.preventDefault();const form=new FormData(event.currentTarget);setLoading(true);setMessage("");try{await localApi("/contact",{method:"POST",body:JSON.stringify({name:String(form.get("name")),email:String(form.get("email")),message:String(form.get("message"))})});setMessage("Inquiry sent.");event.currentTarget.reset();}catch(error){setMessage(error instanceof Error?error.message:"Could not send inquiry.");}finally{setLoading(false)}}return <main className="mx-auto max-w-3xl px-4 py-12 sm:px-6 lg:px-8"><h1 className="text-4xl font-bold text-white">Contact FitSaathi</h1><p className="mt-3 text-zinc-400">Send an inquiry for coach onboarding, dojo approval, or customer support.</p><form onSubmit={submit} className="mt-8 rounded-2xl border border-white/10 bg-white/[0.05] p-6"><input name="name" required placeholder="Name" className="field"/><input name="email" required type="email" placeholder="Email" className="field mt-3"/><textarea name="message" required minLength={10} rows={5} placeholder="Message" className="field mt-3"/><button disabled={loading} className="mt-5 rounded-xl bg-acid px-5 py-3 font-semibold text-ink disabled:bg-zinc-700">{loading?"Sending...":"Send inquiry"}</button>{message?<p className="mt-4 text-sm text-zinc-300">{message}</p>:null}</form></main>}
